---
'@storywright/cli': minor
---

Add `baseBranchDiffDepth` config option and `--base-branch-diff-depth` CLI flag for proper diff detection on the base branch. Fix corrupted favicon base64 data in HTML report.
