---
"@storywright/cli": minor
---

Add `retries` configuration option for flaky test retry

- New `retries` field in `storywright.config.ts` (default: `0`)
- New `--retries` CLI option to override config
